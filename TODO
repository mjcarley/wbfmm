Add:
	normal derivative operators;

"Source-free" calculations (no storage of source data for when sources
greatly outnumber targets)

Field evaluation outside source domain

Test BLAS implementation of rotation operators

Ignore empty boxes (correctly)

vectorized curl and curl/gradient operators, with portable use of SIMD
extensions

AVX vectorisation of single-precision operations
